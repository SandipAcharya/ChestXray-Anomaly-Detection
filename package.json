{
  "name": "minor",
  "version": "1.0.0",
  "description": "X-ray anomaly detection application",
  "scripts": {
    "start:dev": "concurrently \"npm run start --prefix ./backend\" \"npm run dev --prefix ./frontend\"",
    "start:backend": "npm run start --prefix ./backend",
    "start:frontend": "npm run dev --prefix ./frontend",
    "build:frontend": "npm run build --prefix ./frontend",
    "build:backend": "npm run build --prefix ./backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "path-browserify": "^1.0.1"
  }
}
